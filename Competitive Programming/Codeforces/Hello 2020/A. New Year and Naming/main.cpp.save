#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define mp make_pair
#define f first
#define s second
#define pb push_back

int main()
{
	cin.sync_with_stdio(false);
	cout.sync_with_stdio(false);

	cin>>n>>m;
	for(int i=1; i<=n; ++i)
        cin>>v[i];
    for(int j=1; j<=m; ++j)
        cin>>w[i];
    cin>>q;
    while(q--)
    {
        cin>>x;
        int a = 
    }
}
